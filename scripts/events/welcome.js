hexo.on("ready", () => {
  const { version } = require("../../package.json");
  hexo.log.info(`
  ===================================================================
                  木偶の笔记    ${version}
  ===================================================================`);
});
